import { MaterialElevatorDirective } from './material-elevator.directive';

describe('MaterialElevatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialElevatorDirective();
    expect(directive).toBeTruthy();
  });
});
